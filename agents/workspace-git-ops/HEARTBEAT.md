# HEARTBEAT.md

<!-- Git-ops is on-demand; no heartbeat by default. If you add one later, keep it report-only (e.g. git status / fetch) and do not commit or push automatically. -->

Leave empty to skip heartbeat. Optional: add a short checklist for periodic git status/fetch only (no push).
